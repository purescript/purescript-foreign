{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "jshint src && jscs src && pulp build",
    "examples": "jshint src && jscs src && pulp build --include examples"
  },
  "devDependencies": {
    "jscs": "^2.8.0",
    "jshint": "^2.9.1",
    "pulp": "^8.1.0",
    "rimraf": "^2.5.0"
  }
}
